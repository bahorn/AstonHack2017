; astonhack2017 - bh
    INCLUDE "include/VECTREX.INC"   ; includes BIOS definitions.
    ORG 0                           ; sets the code so it knows it's address.
    INCLUDE "res/header.asm"        ; header for the vectrex.
    INCLUDE "defs/all.asm"          ; all of the defs.
    INCLUDE "res/data.asm"          ; Include all the data.
